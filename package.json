{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build:digitalocean": "npm install --production=false && npm run build && npm ci",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
